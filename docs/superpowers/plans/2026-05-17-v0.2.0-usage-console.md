# API Inquiry v0.2.0 Provider Console Implementation Plan

> **For agentic workers:** v0.2.0 scope was revised on 2026-05-17. Historical usage import was removed to keep the app small. Implement only the provider console described here.

**Goal:** Build a lightweight local console with Home and API pages for provider status and API key management.

**Architecture:** Keep the existing balance controller and credential store. Narrow `UsageConsoleViewModel` to provider summaries and API key actions. Remove usage import, parser, storage, and display code from the active app.

**Tech Stack:** Swift 5.9, SwiftUI, AppKit, macOS Keychain, Swift Package Manager, local `APIInquiryCoreTestsRunner`.

---

## Tasks

- [x] Replace usage import tests with provider summary tests.
- [x] Add `APIProviderSummary` to the console view model.
- [x] Remove usage import state and usage file import actions from the console view model.
- [x] Replace the console UI with `Home` and `API` sections only.
- [x] Add an `Add Provider` button on `Home` that opens the API page.
- [x] Remove usage parser/importer/store source files and tests from the active target.
- [x] Update README, release notes, and design docs.
- [x] Verify with `swift run APIInquiryCoreTestsRunner`, `swift build`, and local app reload.
