# API Inquiry v0.3.4 详情页数字颜色实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 根据余额、used-plan 或 remaining-quota 健康度，只给菜单栏展开详情页主模块的大数字上色。

**Architecture:** 将额度阈值语义保留在 `APIInquiryCore` display model 中，SwiftUI 只负责把 display tone 映射成颜色。扩展 `PrimaryProviderDisplayParts` 和 `QuotaWindowDisplayRow` 携带 amount tone，然后只在 `MenuBarContentView` 的主模块数字文本上应用这些 tone。

**Tech Stack:** Swift 5.9, SwiftUI, Swift Package Manager, `APIInquiryCoreTestsRunner`.

---

## 文件结构

- 修改：`Sources/APIInquiryCore/Formatting/ProviderDisplayFormatter.swift`
  - 新增 `ProviderAmountTone`。
  - 给 `PrimaryProviderDisplayParts` 增加 `amountTone`。
  - 给 `QuotaWindowDisplayRow` 增加 `amountTone`。
  - 增加余额、used-plan、remaining-quota 的阈值 helper。
  - 在 `primaryDisplayParts` 中填充 amount tone。
- 修改：`Sources/APIInquiryCore/ViewModels/MenuBarBalanceViewModel.swift`
  - 为 Codex/OpenAI quota rows 填充 `QuotaWindowDisplayRow.amountTone`。
  - 保持 `secondaryProviderRows` 渲染行为不变；row 数据里可以有该字段，但二级 UI 不读取它。
- 修改：`Sources/APIInquiryApp/MenuBarContentView.swift`
  - 将 `ProviderAmountTone` 映射为 SwiftUI `Color`。
  - 只在 `balanceAmountLine` 的 `Text(parts.amountText)` 上应用颜色。
  - 只在 `quotaHeroRows` 的 `Text(row.amountText)` 上应用颜色。
- 修改：`Sources/APIInquiryCoreTestsRunner/MenuBarBalanceViewModelTests.swift`
  - 增加 DeepSeek balance、智谱 used-plan、Codex/OpenAI remaining-quota 的阈值边界测试。
  - 断言现有二级供应商详情行为仍然只是文本展示且不变。

## Task 1: 用测试锁定 Amount Tone Display Model

**Files:**
- 修改：`Sources/APIInquiryCoreTestsRunner/MenuBarBalanceViewModelTests.swift`

- [ ] **Step 1: 在 runner 部分增加测试调用**

在 `MenuBarBalanceViewModelTests.run(using:)` 中已有的主 provider 格式化测试附近加入：

```swift
testPrimaryBalanceAmountToneBoundaries(using: harness)
testPrimaryPlanUsageAmountToneBoundaries(using: harness)
testPrimaryQuotaWindowAmountToneBoundaries(using: harness)
```

- [ ] **Step 2: 增加失败的边界测试**

在已有主 provider 格式化测试附近加入以下 helper 和测试：

```swift
@MainActor
private static func testPrimaryBalanceAmountToneBoundaries(using harness: TestHarness) {
    harness.expectEqual(
        makeViewModel(state: .loaded(.balance(makeSnapshot(total: "50")))).primaryDisplayParts.amountTone,
        .good,
        "balance 50 is good"
    )
    harness.expectEqual(
        makeViewModel(state: .loaded(.balance(makeSnapshot(total: "49.99")))).primaryDisplayParts.amountTone,
        .warning,
        "balance below 50 is warning"
    )
    harness.expectEqual(
        makeViewModel(state: .loaded(.balance(makeSnapshot(total: "10")))).primaryDisplayParts.amountTone,
        .warning,
        "balance 10 is warning"
    )
    harness.expectEqual(
        makeViewModel(state: .loaded(.balance(makeSnapshot(total: "9.99")))).primaryDisplayParts.amountTone,
        .critical,
        "balance below 10 is critical"
    )
}

@MainActor
private static func testPrimaryPlanUsageAmountToneBoundaries(using harness: TestHarness) {
    harness.expectEqual(
        primaryPlanUsageParts(usagePercentage: Decimal(40)).amountTone,
        .good,
        "plan usage 40 is good"
    )
    harness.expectEqual(
        primaryPlanUsageParts(usagePercentage: Decimal(string: "40.01")!).amountTone,
        .warning,
        "plan usage above 40 is warning"
    )
    harness.expectEqual(
        primaryPlanUsageParts(usagePercentage: Decimal(80)).amountTone,
        .warning,
        "plan usage 80 is warning"
    )
    harness.expectEqual(
        primaryPlanUsageParts(usagePercentage: Decimal(string: "80.01")!).amountTone,
        .critical,
        "plan usage above 80 is critical"
    )
}

@MainActor
private static func testPrimaryQuotaWindowAmountToneBoundaries(using harness: TestHarness) {
    let viewModel = makeQuotaToneViewModel(remainingPercentages: [
        Decimal(60),
        Decimal(59),
        Decimal(20),
        Decimal(19)
    ])

    harness.expectEqual(viewModel.primaryQuotaWindowRows.map(\.amountTone), [.good, .warning, .warning, .critical], "quota tone boundaries")
    harness.expectEqual(viewModel.primaryQuotaWindowRows.map(\.amountText), ["60", "59", "20", "19"], "quota amount text preserved")
}
```

- [ ] **Step 3: 增加测试 helper**

在现有 helper 方法附近加入：

```swift
@MainActor
private static func primaryPlanUsageParts(usagePercentage: Decimal) -> PrimaryProviderDisplayParts {
    makeViewModel(
        state: .loaded(.planUsage(PlanUsageSnapshot(
            providerID: .zhipuCodingPlan,
            windowLabel: "5h",
            usagePercentage: usagePercentage,
            resetAt: nil,
            isAvailable: usagePercentage < 100,
            fetchedAt: Date(timeIntervalSince1970: 1_715_000_000)
        )))
    ).primaryDisplayParts
}

@MainActor
private static func makeQuotaToneViewModel(remainingPercentages: [Decimal]) -> MenuBarBalanceViewModel {
    let windows = remainingPercentages.enumerated().map { index, remaining in
        QuotaWindowSnapshot(
            label: index == 0 ? "5h" : "Window \(index)",
            remainingPercentage: remaining,
            resetAt: nil,
            isAvailable: remaining > 0
        )
    }
    return makeViewModel(
        state: .loaded(.quotaUsage(QuotaUsageSnapshot(
            providerID: .codex,
            planName: "Plus",
            windows: windows,
            fetchedAt: Date(timeIntervalSince1970: 1_715_000_000)
        )))
    )
}
```

- [ ] **Step 4: 运行测试确认失败**

运行：

```bash
swift run APIInquiryCoreTestsRunner
```

预期：FAIL，因为 `amountTone` 和 `ProviderAmountTone` 尚不存在。

- [ ] **Step 5: 提交失败测试**

```bash
git add Sources/APIInquiryCoreTestsRunner/MenuBarBalanceViewModelTests.swift
git commit -m "test: cover primary amount color thresholds"
```

## Task 2: 实现 Core Amount Tone 语义

**Files:**
- 修改：`Sources/APIInquiryCore/Formatting/ProviderDisplayFormatter.swift`
- 修改：`Sources/APIInquiryCore/ViewModels/MenuBarBalanceViewModel.swift`

- [ ] **Step 1: 增加 tone 类型和 display model 字段**

在 `ProviderDisplayFormatter.swift` 的 `ProviderStatusTone` 附近加入：

```swift
public enum ProviderAmountTone: Equatable {
    case neutral
    case good
    case warning
    case critical
}
```

在 `PrimaryProviderDisplayParts` 的 `amountText` 后增加 `public let amountTone: ProviderAmountTone`，并在 `QuotaWindowDisplayRow` 的 `amountText` 后增加 `public let amountTone: ProviderAmountTone`。

- [ ] **Step 2: 更新所有 `PrimaryProviderDisplayParts` 构造位置**

占位值使用 neutral tone：

```swift
amountText: "--",
amountTone: .neutral,
```

余额使用 balance tone：

```swift
amountText: amountText,
amountTone: balanceAmountTone(for: balance.totalBalance),
```

used-plan 使用 plan usage tone：

```swift
amountText: formatPercentage(usage.usagePercentage),
amountTone: planUsageAmountTone(for: usage.usagePercentage),
```

quota primary parts 使用 remaining quota tone：

```swift
amountText: formatPercentage(window?.remainingPercentage ?? 0),
amountTone: window.map { remainingQuotaAmountTone(for: $0.remainingPercentage) } ?? .neutral,
```

- [ ] **Step 3: 增加阈值 helper**

在 `ProviderDisplayFormatter` 中现有 formatting helper 附近加入：

```swift
private static func balanceAmountTone(for amount: Decimal) -> ProviderAmountTone {
    if amount >= Decimal(50) {
        return .good
    }
    if amount >= Decimal(10) {
        return .warning
    }
    return .critical
}

private static func planUsageAmountTone(for usagePercentage: Decimal) -> ProviderAmountTone {
    if usagePercentage <= Decimal(40) {
        return .good
    }
    if usagePercentage <= Decimal(80) {
        return .warning
    }
    return .critical
}

public static func quotaWindowAmountTone(for window: QuotaWindowSnapshot) -> ProviderAmountTone {
    remainingQuotaAmountTone(for: window.remainingPercentage)
}

private static func remainingQuotaAmountTone(for remainingPercentage: Decimal) -> ProviderAmountTone {
    if remainingPercentage >= Decimal(60) {
        return .good
    }
    if remainingPercentage >= Decimal(20) {
        return .warning
    }
    return .critical
}
```

- [ ] **Step 4: 填充 quota row amount tone**

在 `MenuBarBalanceViewModel.quotaWindowRows(for:suffixText:)` 中增加新字段：

```swift
amountText: amountText,
amountTone: ProviderDisplayFormatter.quotaWindowAmountTone(for: window),
```

- [ ] **Step 5: 运行测试确认 tone 语义通过**

运行：

```bash
swift run APIInquiryCoreTestsRunner
```

预期：PASS。

- [ ] **Step 6: 提交 Core 实现**

```bash
git add Sources/APIInquiryCore/Formatting/ProviderDisplayFormatter.swift Sources/APIInquiryCore/ViewModels/MenuBarBalanceViewModel.swift
git commit -m "feat: expose primary amount health tones"
```

## Task 3: 只在详情页主模块数字应用颜色

**Files:**
- 修改：`Sources/APIInquiryApp/MenuBarContentView.swift`

- [ ] **Step 1: 增加 SwiftUI 颜色映射**

在现有 `statusColor(for:)` helper 附近加入：

```swift
private func amountColor(for amountTone: ProviderAmountTone) -> Color {
    switch amountTone {
    case .neutral:
        return .primary
    case .good:
        return .green
    case .warning:
        return .yellow
    case .critical:
        return .red
    }
}
```

- [ ] **Step 2: 给单行主数字上色**

在 `balanceAmountLine(parts:amountSize:)` 中只更新 amount 文本：

```swift
Text(parts.amountText)
    .font(.system(size: amountSize, weight: .medium, design: .rounded))
    .foregroundStyle(amountColor(for: parts.amountTone))
    .monospacedDigit()
```

- [ ] **Step 3: 给主模块 quota rows 数字上色**

在 `quotaHeroRows` 中只更新 `Text(row.amountText)`：

```swift
Text(row.amountText)
    .font(.system(size: 36, weight: .medium, design: .rounded))
    .foregroundStyle(amountColor(for: row.amountTone))
    .monospacedDigit()
    .lineLimit(1)
    .minimumScaleFactor(0.8)
    .frame(minWidth: 42, alignment: .trailing)
```

- [ ] **Step 4: 检查二级 row 代码不变**

确认 `secondaryProviderDetail(for:)` 仍然不调用 `amountColor(for:)`，也不读取 `quotaRow.amountTone`。

运行：

```bash
rg -n "amountColor|amountTone" Sources/APIInquiryApp/MenuBarContentView.swift
```

预期：`amountColor` 只出现在 helper、`Text(parts.amountText)`、以及 `quotaHeroRows` 中的 `Text(row.amountText)`。

- [ ] **Step 5: 构建并测试**

运行：

```bash
swift run APIInquiryCoreTestsRunner
swift build
```

预期：两个命令都 PASS。

- [ ] **Step 6: 提交 SwiftUI 渲染改动**

```bash
git add Sources/APIInquiryApp/MenuBarContentView.swift
git commit -m "feat: color primary detail amount values"
```

## Task 4: 最终验证

**Files:**
- 仅验证，无必须文件改动。

- [ ] **Step 1: 查看最终 diff**

运行：

```bash
git status --short
git log --oneline -5
git show --stat --oneline HEAD
```

预期：三个实现 commit 后工作区干净。

- [ ] **Step 2: 运行完整验证**

运行：

```bash
swift run APIInquiryCoreTestsRunner
swift build
```

预期：两个命令都 PASS。

- [ ] **Step 3: 如果桌面会话可用，做手动 app 检查**

运行：

```bash
Scripts/build-local-app.sh
```

预期：本地 app 构建成功。通过正常本地流程打开 app，确认只有展开详情页主模块的大数字出现颜色变化。

---

## Self-Review

- Spec coverage：计划实现 balance、used-plan、remaining-quota 三类主模块大数字颜色，并明确不影响菜单标题、状态、二级列表和 Console。
- Placeholder scan：没有保留占位任务。
- Type consistency：`ProviderAmountTone`、`PrimaryProviderDisplayParts.amountTone`、`QuotaWindowDisplayRow.amountTone` 都先定义再使用。
