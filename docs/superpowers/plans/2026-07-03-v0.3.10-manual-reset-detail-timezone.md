# v0.3.10 Manual Reset Detail and Timezone Implementation Plan

> **For agentic workers:** Keep this branch surgical. Every changed line should map to the four review fixes, manual reset details, or timezone adaptation.

**Goal:** Fix the priority review issues, add a Console-styled manual reset detail sheet, and make manual reset times follow the system timezone.

**Branch:** `v0.3.10`

## Global Constraints

- Preserve v0.3.9 behavior: manual reset credits remain Console Home-only, independently cached, independently refreshed, and non-blocking for Menubar.
- Do not query manual reset credits from Menubar refresh.
- Do not implement the manual reset action itself.
- Do not expose tokens, cookies, complete account IDs, or raw response payloads.
- Keep the detail UI visually consistent with the existing Console: dark material, compact table, light separators, restrained controls.
- Use tests before implementation for data parsing and view-model/display-model behavior.

## Steps

- [x] **Step 1: Documentation drift**
  - Update `README.md`, `README_zh.md`, and `README_en.md` from `PASS: 507 expectations` to the current passing count.
  - Remove or reword stale “Codex current plan display” scope where the Home card now uses manual reset credits.
  - Verify with a focused `rg` pass.

- [x] **Step 2: Zhipu percentage parsing**
  - Add a failing `ZhipuCodingPlanProviderTests` case where `percentage` is `17.4`.
  - Change `ZhipuQuotaLimit.percentage` to `Decimal`.
  - Keep snapshot `usagePercentage` as `Decimal` and compare availability against `Decimal(100)`.

- [x] **Step 3: Codex reset_at tolerance**
  - Add failing tests for `reset_at` as Unix milliseconds and ISO-8601 string.
  - Decode `reset_at` to `Date?` using seconds, milliseconds, numeric strings, and ISO-8601 strings.
  - Keep `reset_after_seconds` fallback unchanged.

- [x] **Step 4: Manual reset cache cleanup**
  - Add a failing `UsageConsoleViewModelTests` case proving Codex removal clears `codexManualResetCreditsState`.
  - Reset manual reset state and refresh flag after successful `removeProvider(.codex)`.
  - Do not clear unrelated providers or user settings.

- [x] **Step 5: Timezone-aware formatter/display model**
  - Expose the display calendar/timezone from `LastRefreshTimeFormatter` or equivalent injected time context.
  - Remove `UsageConsoleViewModel.beijingCalendar`.
  - Add manual reset detail display rows with granted/expiry text using `yyyy-MM-dd HH:mm:ss UTC±HH:MM`.
  - Add tests for Asia/Shanghai and America/Los_Angeles.

- [x] **Step 6: Console-styled detail sheet**
  - Add a value-area action to `ProviderMetricItem` without wrapping the whole metric or nesting the accessory refresh button.
  - Wire `UsageConsoleHomeSection` to open a sheet from `UsageConsoleView`.
  - Add `CodexManualResetCreditsDetailSheet` using the existing Console visual language.
  - Keep refresh icon placement and height alignment unchanged.

- [x] **Step 7: Verification**
  - Run `swift run APIInquiryCoreTestsRunner`.
  - Run the macOS app build.
  - Inspect the final diff for unrelated changes and untouched user files.
